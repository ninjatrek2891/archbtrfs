---
- name: Enable sshd service
  ansible.builtin.command:
    cmd: >-
      arch-chroot /mnt
      systemctl enable sshd

- name: Allow temporary passwordless sudo for setup user
  ansible.builtin.copy:
    dest: "/mnt/etc/sudoers.d/99-{{ setup_username }}-temp"
    content: "{{ setup_username }} ALL=(ALL) NOPASSWD: ALL\n"
    owner: root
    group: root
    mode: "0440"

# - name: Ask for confirmation
#   ansible.builtin.pause:
#     prompt: "Do you want to reboot? (yes/no)"
#   register: i_reboot_reboot_confirm

# - name: Abort if not confirmed
#   ansible.builtin.fail:
#     msg: "Reboot aborted by user"
#   when: i_reboot_reboot_confirm.user_input | lower != "yes"

- name: Shutdown machine
  ansible.builtin.command:
    cmd: shutdown now
  async: 1
  poll: 0

- name: Send message
  ansible.builtin.debug:
    msg: "System wil reboot now, please enter LUKS password"
